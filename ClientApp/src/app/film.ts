export interface Film {
  pictureSrc: string;
  filmName: string;
}
// Will be turned into full film interface after creating database model.
// Should I create "app/model" directory for this purpose or "app/film.ts" is just fine?
